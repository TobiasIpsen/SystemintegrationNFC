BEGIN;

-- As taken from Student-class in Shared Class Library in EventManagement Solution
CREATE TABLE IF NOT EXISTS Students (
	Id INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
	Name VARCHAR(255) NOT NULL,
	UserClass VARCHAR(255),
	CardId VARCHAR(255) NOT NULL,
	Image VARCHAR(255)
);

SAVEPOINT TableCreated_TestUserNotInputted;

INSERT INTO Students (Name, CardId)
SELECT 'Testy McTester', '0000000000000000000000000000000000000000'
VALUES
--('Testy McTester','0000000000000000000000000000000000000000')
WHERE NOT EXISTS (
	SELECT 1 FROM Students WHERE CardId = '0000000000000000000000000000000000000000'
);

SAVEPOINT TestUserInputted_EventTableNotYetMade;

-- As taken from Event-class in Shared Class Library in EventManagement Solution
CREATE TABLE IF NOT EXISTS Events (
	id INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
	Name VARCHAR(255) NOT NULL,
	CreatedAt TIMESTAMP WITH TIME ZONE NOT NULL
);

COMMIT;


--ROLLBACK;
